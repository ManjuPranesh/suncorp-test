--DROP TABLE MONTHLY_MASTER_BK_CHANGED_ATTR_INTERVIEW;

CREATE TABLE MONTHLY_MASTER_BK_CHANGED_ATTR_INTERVIEW (
    nvic VARCHAR(255) DEFAULT NULL,
    old_new_flag VARCHAR(255) DEFAULT NULL,
    family VARCHAR(255) DEFAULT NULL,
    year1 VARCHAR(255) DEFAULT NULL,
    bodystyle VARCHAR(255) DEFAULT NULL,
    country VARCHAR(255),
    turbo VARCHAR(255),
    fuel VARCHAR(255),
    drive VARCHAR(255),
    valid_data int DEFAULT NULL,
    ID INT NOT NULL,
    PRIMARY KEY (ID)
);
